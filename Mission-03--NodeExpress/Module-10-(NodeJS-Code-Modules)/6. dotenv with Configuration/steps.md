
### run commands:
1. **"npm init -y"** to create package.json
2. **"npm install dotenv"** to install dotenv package
3. cerate .env file
4. create .gitignore file
 - add files that should be ignored by git
5. create config.js 
